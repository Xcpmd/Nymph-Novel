# 2026-09-22 工作日志

## 修复工作台第一步方向不显示，并支持手写大纲跳过第二步

用户报告：创作工作台第一步「确定故事发展方向」下，AI 生成的内容不会被填充到界面；如果方向是自己手写的，应该跳过第二步「检视并修改故事大纲」。

### 缺陷：模型返回的数组被包了一层，界面一条都显示不出来

- 排查手法是从 `generation_runs` 表里读真实模型输出。该表保存了每次生成的 `partial_text`，对照它一眼就能看出模型到底返回了什么。
- 实测三条 direction 记录，模型返回的全是**对象**而不是提示词要求的数组：
  - `{"directions":[...]}`
  - `{"type":"json_object","directions":[...]}`
  - `{"type":"json_object","content":{"directions":[...]}}`
- 前端 `directionOptions` 原先写的是 `if (!Array.isArray(data)) return []`，拿到对象就返回空数组，于是方向卡片一张都不渲染。用户描述为「AI 生成的大纲不会被填充」。
- 修复分两层：服务端在发送 json 事件前把结果归一成数组；前端再走一遍同样的归一化，兼容早期记录与自定义提示词的其他包裹形式。

### 新增：方向归一化与记录数组提取

- `normalizeOptionList` 把各种包裹形式收敛成选项数组，字段名兼容 `title`/`name`/`heading` 与 `summary`/`content`/`description`。
- `extractRecordList` 是更通用的下钻取数组，候选键按 `directions`、`options`、`items`、`entries` 等顺序尝试，都落空时逐层下钻对象找第一个可解析的数组。
- 顺手修掉同一根因的两处隐患：百科条目抽取与角色抽取原先都用 `extractJson<Array<...>>`，模型返回 `{entries:[...]}` 或 `{characters:[...]}` 时同样会整批丢弃，现已切到 `extractRecordList`。

### 新增：手写大纲直接充当事件大纲，跳过第二步

- ① 里点「采用这个方向」会记下 `selectedDirection`，手动改动输入框会清空它。据此判断来源：`selectedDirection` 为空且输入框有内容即为手写。
- 手写时按钮文案变为「以手写内容为大纲，直接生成正文」，点确认后直接把内容建成事件并跳到第三步，不再调用 AI。
- `src/lib/markdown/outline.ts` 承担步骤推导：按有序列表、无序列表、小节标题、自然段的优先级取一种结构作为推进步骤，列表项之后的缩进内容收作步骤说明，提要取首句并在过长时截断，步骤数上限二十四个。
- 事件标题优先取一级标题，其次取首个非空行。
- 推导挂载点在 `story-events` 的 create：没有显式传入 `steps` 时自动从 `outline` 推导，推导不出则报错提示补充内容。

### 踩到的坑

- 有序列表正则把序号写成了捕获组，`match[1]` 拿到的是数字而不是内容，步骤提要全变成「1」「2」。序号要改用非捕获组 `(?:\d+)`。
- 「未知包装时逐层下钻」第一版只处理数组值，遇到 `{payload:{items:[...]}}` 这种两层对象仍然取不到，改为对象值也继续下钻。
- 沙箱的批量删除保护再次拦截构建：`next build` 清理 `.next` 时报 `SAFE_DELETE_BULK_CONFIRM_REQUIRED`。**根因是用户自己启动的 `npm run dev` 正占着 `.next`**，重命名和删除都会 EPERM。判断依据是 `Get-CimInstance` 列出 node 命令行，看到 `next dev -p 5180` 与它的 worker 进程。这种情况不要杀用户的服务，改用它验证即可。
- 试过用目录联接把 `node_modules` 复用到隔离工程里跑构建，Turbopack 拒绝指向工程外的链接，报 `Symlink is invalid, it points out of the filesystem root`。已经放弃这条路，拆链接时用 `[System.IO.Directory]::Delete` 保证只移除链接不碰目标，拆后核对 `node_modules` 顶层条目数仍为 403。
- 本机 PowerShell 工具不回显 stdout，改用 `Get-CimInstance` 写文件再读，或用 bash 核对。

### 验证结果

- `npm run typecheck` 无错误。
- `npm run lint` 零错误零警告。
- `npm run test` 一百五十四项通过，新增方向归一化十一项与大纲推导十四项。
- `npm run check:i18n` 六百二十一键，缺失零。
- `npm run smoke` 八十项通过，新增手写大纲相关四项。
- 真实调用实测：建临时小说后发起一次 direction 生成，服务端下发的 json 事件顶层类型为数组、含三个选项，标题说明基调控风险齐备，修复前这里下发的是对象。
- 手写大纲实测：一份六条目的手写大纲建成事件后自动推导出六个步骤，提要均为内容而非编号，进度指针为零，刷新读回一致。
- 生产构建未能在原工程执行，原因是用户的 dev server 占用 `.next`。类型检查、lint、单测、冒烟与真实接口验证均已覆盖编译与运行路径。

### 用户既有数据

- 用户小说《啥龙》的既有事件「公寓里的猫」完好：大纲一千九百九十二字，九个推进步骤，状态 active，进度零。修复后在工作台第二步可以正常看到。

## 修复角色发言标记不渲染，配色接上角色图鉴，并新增角色名开关

用户报告：正文里的 `<talk chara="陨星">现在不怕了？</talk>` 渲染不出来，应当显示为带引号并按角色图鉴配色染色的对白，另外角色名前缀要能用按钮控制显隐。

### 缺陷：标记停在纯文本状态，从未变成元素

- 整条链路原本是这样的：`talkToMarkedText` 把标记改写成私有区字符定界的文本，再由 `MarkdownView` 的 `components.code` 还原成带样式的对白。
- **断点在于那段文本没有转成 Markdown 行内代码**，没有反引号就不会产生 `code` 元素，`components.code` 永远不被调用，私有区字符于是作为普通文本留在段落里。结果是既没有引号也不上色。
- 十六项 `talk.ts` 纯函数测试全绿也发现不了这个问题，因为它们只覆盖解析与替换，不经过 Markdown 渲染。
- 曾考虑给 `talkToMarkedText` 补反引号，但行内代码不能跨行，内容里的反引号还会破坏定界，小说对白这两种情况都会遇到，因此放弃。

### 修复：改用 rehype 插件在清洗前把标记转成元素

- 新增 `src/lib/markdown/talk-rehype.ts`，递归遍历 hast 树，把含私有区字符的文本节点拆成 `span[data-chara]`。
- 插件顺序不能颠倒：必须挂在 `rehype-sanitize` **之前**，否则私有区字符会被当普通文本留下。
- 不进入 `code` 与 `pre` 子树，代码块里的尖括号示例保持原样。
- 标记不完整或角色名为空时按普通文本保留，宁可少染一处颜色也不能吞掉正文。
- `MarkdownView` 改为用 `components.span` 读取 `data-chara`，用 `speakers` 映射取色相，取不到才回落到名称哈希。
- 白名单同步放行 `span` 的 `talk-line` 类名与 `data-chara` 属性，其余一律剥离。色相通过 React 的 style 下发，不进 sanitize 白名单，避免开放 style 带来的注入面。

### 配色接上角色图鉴

- 新增 `src/lib/markdown/speakers.ts` 的 `buildSpeakerColors`，把角色名与**全部别名**映射到同一色相。
- 此前 `MarkdownView` 已经支持 `speakers`，但三个调用点都没传，实际一直走哈希兜底，等于图鉴里配的颜色从未生效。
- 接上的调用点：阅读页 `ChapterDetailClient`、工作台 `WorkbenchClient`、编辑器预览 `MarkdownEditor`，三处都新增加载角色列表。

### 角色名开关

- `app_settings` 新增 `showSpeakerName`，默认开启，`/api/settings` 的 PATCH 同步放行。
- 角色名前缀由 CSS 的 `::before` 搭配 `attr(data-chara)` 生成，因此选中复制只会得到纯对白。
- 隐藏通过容器上的 `talk-names-hidden` 类整体关掉，颜色与引号不受影响。
- 快捷按钮放在阅读页正文预览的标题栏与工作台生成区，两处都写回同一设置项；设置页也补了开关。

### 验证结果

- `npm run typecheck` 无错误。
- `npm run lint` 零错误零警告。
- `npm run test` 一百八十五项通过，新增渲染链路十四项、rehype 转换十二项、配色映射五项。
- `npm run check:i18n` 六百二十五键，缺失零。
- `npm run smoke` 八十项通过。
- **真实渲染验证**：新增 `MarkdownView.test.ts` 用 `renderToStaticMarkup` 直接渲染组件，断言最终 HTML 含 `talk-line`、`data-chara`、`--talk-hue:210` 与 `“现在不怕了？”`。这是本项目第一次覆盖组件渲染层，此前的纯函数测试挡不住这类缺陷。
- **真实数据验证**：用无头 Edge 转储用户《啥龙》第 1 章的 DOM，72 个标记对应 72 个发言元素，`data-chara` 与 `--talk-hue` 分别为顾羽落 193、陨星 43，与角色图鉴一致，私有区字符与 talk 标签零残留。
- **视觉验证**：截图确认开启时显示「陨星：“现在不怕了？”」且按角色着色，关闭时角色名消失而颜色与引号保留；截图存于 `H:/workbuddy/_nymph-talk-preview/`。

### 本轮的坑

- 无头 Edge 截图与 DOM 转储都要加 `--virtual-time-budget`，否则拿不到异步加载后的正文；`--dump-dom` 配合它才能看到客户端渲染的结果。
- 截图路径用反斜杠会失败，改用正斜杠即可。
- 一条断言写成 `expect(html).not.toContain('chara=')`，但 `data-chara=` 本身含 `chara=`，改为断言不含 `<talk` 与 `</talk>`。
- vitest 5 用 oxc 转译，配置 `esbuild.jsx` 会被忽略并告警；实测 oxc 默认已支持自动 JSX 运行时，删除该配置即可。

## 视觉改版、沉浸阅读、设定页保存整合与文件回收站

用户要求：加文字阴影、模块通透质感、动态流体背景加模糊、模块发光、补上下拉菜单、做出高级感；加沉浸式阅读；设定页两个保存按钮合并为一个并常驻屏幕、消除保存闪动；删除文件改为移入 public 下的回收目录。

### 通透质感与发光体系

- 玻璃拟态的关键是三件套：半透明底、`backdrop-filter` 模糊身后、**内侧高光描边**。只做前两项会显得发灰发脏，第三项才是通透感的来源。
- 新增令牌：`--glass-bg`、`--glass-border`、`--glass-blur`、`--glass-highlight`、`--glow-soft/glow-accent/glow-strong`、`--glow-ring`、`--text-shadow-*`。
- `.card`、`.btn`、`.input`、`.chip`、`.nav-item` 全部改走玻璃面；`.card::before` 加一道渐隐亮线模拟玻璃上缘折射。
- 主按钮改主题色渐变加辉光，hover 时辉光加强。

### 动态流体背景

- 纯 CSS 实现：四枚大面积模糊光斑在视口内缓慢漂移缩放，色相全部由 `--accent-hue` 派生，换主题色时背景一并改变。
- 上层盖一层与底色同调的薄雾，把光斑压到刚好能看见，保证正文对比度稳定。
- **第一版饱和度开到 88%、透明度 0.55，出来是糖果色，很廉价**。降到 62% 与 0.34 并加大模糊半径后才像样。高级感来自低饱和与克制，不是色彩本身。
- 光斑数量固定、动画只走合成器，不占主线程；`prefers-reduced-motion` 下动画自动停下，静止色块仍然保留。

### 文字阴影的方向

- 浅色主题下**白色文字阴影等于没有**。第一版用了白色描边，截图上完全看不出效果。
- 改为以极淡暗色托底、白色仅提亮边缘，全站由 body 统一挂载。

### 自定义下拉菜单

- 原生 select 的展开列表由操作系统绘制，不参与站内视觉，一展开就割裂。
- 改写 `primitives` 的 Select 为按钮加浮层，**保持原生用法**：接收 option 子元素，onChange 派发带 `target.value` 的对象，14 个调用点一行未改。
- 支持键盘上下键、Enter 选中、Esc 收起、点击外部关闭，带 `role="listbox"` 与 `aria-expanded`。
- 浮层宽度用 `min-w-full` 而非左右对齐，否则长标签会被截断。

### 沉浸式阅读

- 新增 `ImmersiveReader`，整层覆盖视口，只留正文与一条工具条，可调字号与宽度、切换角色名显隐。
- **必须用 Portal 渲染到 body**。页面主体带 `relative z-10` 形成层叠上下文，就地渲染的固定层会被顶部导航盖住，这是第一版的实际表现。
- 与地址栏 `#immersive` 同步，链接可直达沉浸阅读，用 `replaceState` 避免留下多余历史记录。
- Esc 退出，进入时锁定底层页面滚动。

### 设定页保存整合

- 原先头部与底部各有一个保存按钮，合并为底部居中的悬浮保存条，滚动到任何位置都能直接保存。
- **闪动的根因是保存成功后回写服务端返回值**：本地状态就是用户正在编辑的那一份，用响应覆盖会让输入框内容与光标位置跳变。改为不回写，服务端归一产生的差异留到下次进入页面时同步。
- 保存状态提示收进悬浮条，并用短暂的成功态加超时复位，避免文案反复切换。

### 文件回收站

- 新增 `src/lib/store/trash.ts`，删除改为按时间戳重命名后移入 `public/.trash`，归档名形如 `20260922-223314-chapter-content.md`。
- 覆盖三处：章节正文、整部小说目录、迁移时产生的重复文件。迁移会先归档再丢弃，万一内容与目标不同也有据可查。
- 空目录清理保持原样，空目录没有内容可恢复，留着反而会在地盘上堆出成片空壳。
- 跨盘符改名会报 EXDEV，已退回复制后删除。
- 归档失败只告警不抛错：删除流程本身不可逆，不该因为归档失败让界面显示错误而数据库其实已经清掉。
- `public/.trash/` 已加入 `.gitignore`。

### 验证结果

- `npm run typecheck` 无错误，`npm run lint` 零错误零警告。
- `npm run test` 一百八十五项通过。
- `npm run check:i18n` 六百二十七键，缺失零。
- `npm run smoke` 八十项通过。
- 回收站实测：删章节后正文归档且原位置已移走、内容完整；删整部小说后目录整体归档。测试产物已清理。
- 视觉实测：明暗两套主题、沉浸模式、下拉展开态分别截图确认，截图存于 `H:/workbuddy/_nymph-talk-preview/`。

### 排查手法

- 用户上一轮启动的 dev server 已停止，本轮自己起了 5190 做验证，用 `NYMPH_BASE` 指定冒烟目标端口，避免与他人的服务互相干扰。
- 验证下拉展开态这类需要交互的场景，临时把组件初始状态改为展开、截图后立刻改回，比引入浏览器自动化轻得多。改回后要用 grep 确认真的恢复了。
- 层叠上下文导致的覆盖失效不容易从代码上看出来，截一张图就能立刻发现。

## 书架改为竖版封面卡

用户要求：每本书用竖版矩形封面，常态只显示封面不显示其余任何元素，鼠标悬浮时封面背景放大而外框不变、下边框出现由下至上的黑色渐变、渐变上显示书名作者章数日期，点击进入阅读页。

### 已有的基础

- `coverImage`、`coverFit`、`coverEmoji` 三个字段与封面编辑弹窗早已存在，弹窗里已支持本地上传与 emoji 降级，这部分没有改动。
- 缺的只是书架的呈现方式：原先是横版信息卡，封面缩减成左上角一个小图标，其余全是标题摘要与统计标签。

### 竖版封面卡

- 新增 `.cover-card` 系列样式，宽高比锁 `2 / 3`，圆角、玻璃边框与外发光沿用既有令牌。
- 常态只有封面。元信息放进 `.cover-card-info`，默认 `opacity: 0` 并下沉，悬浮时才浮出。
- **放大作用在媒体元素而不是容器上**，因此外框尺寸始终不变，观感像镜头推近而不是卡片被撑大。
- 黑色渐变压在图片上，中段就开始透明，保证只在底部压暗而不是整张发灰。
- 操作按钮放在右上角圆形玻璃底上，与底部元信息分开，避免挤占。
- 点击封面进阅读页，操作按钮另开，因此按钮不能嵌在链接里。

### 悬停状态必须挂在外层容器

- 操作按钮与封面链接互为**兄弟节点**，若把 `:hover` 挂在卡片链接上，指针移到按钮时卡片就不再处于悬停态，按钮会在被点中之前消失。
- 因此所有悬停规则统一挂在 `.cover-group` 这一层，并同时用 `:focus-within` 覆盖键盘操作。
- 触摸设备没有悬停能力，用 `@media (hover: none)` 让元信息常显，桌面端不受影响。

### 完整显示模式的衬底

- 用户在验证期间给《啥龙》传了一张横向封面并选了完整显示，在竖版框里上下留下大片空边。
- 增加 `.cover-card-blur`：把原图放大模糊后垫在底层补满画面。它不参与悬停放大，否则背景构图会跟着一起动。

### 验证手法

- 截图悬停态仍然用临时覆盖样式：先把 CSS 备份，再追加一段让悬浮层常显的规则，截完用备份恢复。
- 恢复后必须 grep 确认临时标记已清除、正式样式仍在，这次核对到 17 处正式选择器。

### 验证结果

- `npm run typecheck`、`npm run lint` 均无问题，`npm run test` 一百八十五项通过，`npm run check:i18n` 六百二十七键缺失零，`npm run smoke` 八十项通过。
- 造了两本演示小说分别覆盖有封面与仅 emoji 两种情况，正常态与悬停态各截一张确认，随后连同回收站归档一并清理。
- 期间发现用户角色的数量由十五变成二。核对 `deleteNovel` 只按 id 删 novels 表、其余表靠外键级联，且测试小说删除后没有孤儿角色残留，确认是用户自己的操作，不是本轮改动引起的。

## 新增界面字号设置

用户反馈默认文字太小看不清，要求设置里能调。

### 做法：用根字号带动全站等比缩放

- 新增 `uiFontScale` 设置项，**默认 1.15**，可调区间 0.85 到 1.4，由 `SettingsProvider` 写到根节点的 `--ui-font-scale`。
- `html` 的字号改为 `calc(100% * var(--ui-font-scale, 1.15))`。全站尺寸大多以 rem 计量，因此改根字号即可让文字与间距一起放大，不必逐个组件改字号。
- 用百分比而不是固定像素，能尊重用户浏览器的字号偏好。

### 关键：body 的固定像素必须改成相对单位

- `body` 原本写死 `font-size: 15px`，它会**覆盖根字号的继承**，导致大量没有显式设字号的文字完全不参与缩放。
- 改为 `0.9375rem`，等于原来的 15px，但会跟着根字号一起变。这一步不做的话，设置看上去只对一半元素生效。

### 避免首屏跳动

- 界面字号倍率随服务端设置以 inline style 直接输出在 `html` 上，首屏即按用户设定渲染。
- 若交给客户端设置，页面会先用默认倍率画一遍再跳到实际值，与主题闪烁是同一类问题。

### 验证结果

- 后端读写与边界实测：默认 1.15；越界值 2 夹到 1.4、0.1 夹到 0.85；1.17 保留两位小数；改后重新读取一致。
- 一百与一百一十五两个倍率各截一张同页面对比，文字放大效果明确。
- `npm run typecheck`、`npm run lint` 无问题，一百八十五项测试通过，文案六百二十九键缺失零，冒烟八十项通过。

### 本轮的坑

- 无头 Edge 偶发启动失败，同一条命令重试一次即可，不必怀疑参数写错。




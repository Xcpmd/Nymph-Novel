# AI 网络小说生成与管理项目提示词

## 项目目标

使用 Next.js 构建一个用于 AI 生成网络小说的高效、易管理项目。每部小说独立存储。小说正文以 Markdown 格式输出并渲染。项目为个人应用，单用户使用，不存在服务器托管场景，全部数据保存在本地。

## 技术栈与运行环境

- 使用 Next.js。
- 使用 Tailwind CSS、Font Awesome 等标准化库。
- 使用 TypeScript、ESLint、Prettier 确保代码质量。
- 遵循规范的开发流程。
- 任意文本不使用括号进行解释，使用规范化文本准确描述。
- 仅在本地运行，不部署到服务器，不涉及多用户与权限体系。

## 数据存储

采用本地数据库与本地 Markdown 文件混合方案。

- 本地数据库存储元数据、索引、标签、书签、配置、加密后的 API Key、检索索引、设定、世界观、角色、大纲、生成规则、概括、时间轴、角色图鉴、角色关系网、百科全书等结构化或半结构化数据。
- 本地 Markdown 文件仅存储章节正文，每章一个 Markdown 文件。
- 每部小说独立存储，互不影响。
- 调用 AI 生成时统计 token 用量，并保存统计结果。

## 目录结构

小说正文按照卷与章分级组织。只有在超大型事件结束时才分卷。每章一个 Markdown 文件。

///text
novel/{novelId}/
  卷-001/
    章-001/
      正文.md
    章-002/
      正文.md
  卷-002/
    章-001/
      正文.md
    ...
///

## 小说创作流程

1. 新建小说时，用户手动填写设定与世界观。
2. 用户手动设定主角与配角。
3. 用户手动设定故事大纲。
4. 用户可编写自定义生成规则。生成规则包括文风、是否允许 H 内容等。
5. 开始生成时，AI 根据以往内容给出几个故事发展方向供用户选择。
6. 当用户对所有选项都不满意时，允许用户自行输入故事发展方向。
7. AI 列出故事大纲供用户查看。
8. 用户查看故事大纲期间，可以指出想修改的位置，并要求 AI 根据需求修改。
9. 用户确认后，AI 开始生成正文。
10. 正文生成后，用户可以随时修改不满意的地方。
11. 每章正文生成完成后，附带本章概括以及对总体剧情发展的影响，供后续续写 AI 在上下文中引用。
12. 概括的详细程度根据情节重要性确定。

## 章节与正文

- 支持按章节生成小说。
- 章节字数可配置，配置项包括目标字数与允许容差。
- 支持重新生成、局部重写、续写、扩写、缩写、暂停与恢复。
- 仅正文使用 Markdown 格式。
- 其余数据使用能高效储存的格式。

## 时间轴

- 项目附带时间轴，使用小说内的历法将各个事件串联起来。
- 时间轴记录的是小说内的情节，不是写作过程的时间轴。
- 时间字段放入章节概括中，不单独存储于时间轴文件。
- 时间格式使用小说内的历法。
- 如果存在平行宇宙或穿越情节，时间轴支持分叉与跳转。
- 时间轴支持分支合并、回到主线、跨分支引用。
- AI 起大纲时应浏览往期事件，尤其当前剧情正在经历的情节。
- 具体做法：将时间轴发送给 AI，AI 自主判断剧情相关性，并选择需要详细读取的往期情节，以节省 token。

## 角色系统

- 角色图鉴：记录称呼、性格、能力体系、伏笔管理等信息。
- 角色关系网：记录角色之间的关系。
- 角色关系网渲染为网状图。
- 点击网状图中的角色可跳转至角色图鉴。
- 角色图鉴与角色关系网存储在本地数据库中，并可在界面中查看与编辑。

## 百科全书

- 为每部小说添加一个百科全书。
- 将小说中出现的地名、种族、动植物等储存。
- 根据文章内容生成对应目录。
- 百科全书存储在本地数据库中，并可在界面中查看与编辑。

## 上下文管理

上下文管理以效果优先为原则，采用分层与按需加载结合的方案。

- 始终注入：小说设定与世界观、主角与配角、故事大纲、生成规则、角色图鉴与角色关系网的相关片段、百科全书相关片段。
- 始终注入：完整时间轴，供 AI 自主判断剧情相关性。
- 按需注入：AI 根据时间轴判断相关性后，选择需要详细读取的往期章节。
- 分层摘要：近期章节默认注入较详细概括，较远章节默认注入精简概括，核心事件概括始终保留。
- 摘要分级：概括的详细程度根据情节重要性确定，重要情节保留更完整的细节。
- 检索增强：全文检索索引用于辅助上下文选择，提高相关章节召回率。
- Token 控制：对注入内容设置上限，超出时按相关性与重要性裁剪。

## 提示词模板与生成控制

- 提供默认提示词模板，覆盖故事发展方向生成、故事大纲生成、故事大纲修改、正文生成、正文修改、章节概括生成、时间轴更新等任务。
- 支持按模型定制提示词模板。
- 支持用户覆盖系统提示词与用户提示词。
- 模板与生成规则分离，生成规则由用户编写，模板由项目提供并允许覆盖。
- 生成控制包括重新生成、局部重写、续写、扩写、缩写、暂停与恢复。

## 模型接入与设置

- 在页面设置中配置模型接入与 API 管理。
- 预设几款常见写小说 AI：DeepSeek、GLM、MiMo 等。
- 预置模型与参数。
- 用户也可以自己配置供应商。
- 配置项包括供应商、模型名称、API Key、API 地址、模型参数、流式输出、错误重试、token 统计等。
- API Key 加密存储于本地数据库。
- 支持在设置中切换当前使用的模型。
- 调用 AI 生成时统计 token 用量。

## 导入导出

- 支持导入小说。
- 支持导出小说。
- 支持将小说导出为标准 txt 小说文件。
- 导出内容按卷与章顺序组织，包含章节标题与正文。

## 搜索与导航

- 支持章节搜索。
- 支持全文检索。
- 支持标签。
- 支持书签。
- 支持目录跳转。
- 标签与书签的作用范围是单部小说内。

## 用户界面

- UI 风格以简约平面化为主。
- 文字具有柔和阴影。
- 整体以黑白色调为主，点缀少量主题色。
- 页面设置 hue 滑块，可调整主题色。
- 使用响应式布局，同时支持移动端与 PC 端。
- 所有组件具有过渡动画。
- 支持 Markdown 格式小说的渲染。
- 关系网渲染为网状图，点击角色可跳转至角色图鉴。
- 百科全书根据文章内容生成对应目录。

## 国际化

- 页面仅提供中文。
- 将所有翻译内容集中放入一个 zh.ts 文件内。
- 有翻译需求时，复制一份 zh.ts 并修改为对应语言文件，例如复制为 en.ts 并将内容翻译为英文。
- 在页面配置中可以直接启用对应语言页面。

## 工程质量

- 使用 TypeScript、ESLint、Prettier。
- 遵循规范的开发流程。
- 对关键逻辑提供必要的测试与校验。

## 安全与隐私

- Markdown 渲染需做好 XSS 防护。
- API Key 需加密存储。
- 本地数据需保证隐私，不对外上传。